# heavy build state — re-created by the forge; never committed
/build_output/
out/
*.log
*.rej
*.orig
.DS_Store
Ether_Stock_ROM_*.zip

# personal, per-checkout build settings (EXTRA_OPTIONS etc) -- never published
/device.conf.local
